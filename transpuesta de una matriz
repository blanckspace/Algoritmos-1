#define FIL 10
#define COL 10


void transpuesta(int mat[][],int n,int m)
{int i,j,aux;
 for(i=0;i<n-1;i++)
  {for(j=j+1;j<m;j++)
   {if(j>i)
    {aux=mat[i][j];
     mat[i][j]=mat[j][i];
     mat[j][i]=aux;
    }
   }
  }
}
