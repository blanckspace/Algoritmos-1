
#include <iostream>
#include "stdafx.h"
#include"conio.h"
#include <string>

using namespace std;

int mayusculas(string frase)
{int i,tam,c=0;
tam=frase.length();
for(i=0;i<n;i++)
 {if((frase[i]>=65)&&(frase[i]<=90))
  c++;
 }
 return c;
}
